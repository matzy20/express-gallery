<!DOCTYPE html>
html(lang="en")
head
  link(rel="stylesheet" href="/styles/styles.css")
  meta(charset="UTF-8")
  title Gallery
body
  h1 Gallery
  each gallery in galleries
    div(class="container")

    h2 Author: #{gallery.author}
    h2 Id: #{gallery.id}
    h2 Link: #{gallery.link}
    image(src='#{gallery.link}')

    p Description: #{gallery.description}

